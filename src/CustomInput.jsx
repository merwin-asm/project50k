import React from 'react'
import './ScenarioForm.css'

export default function CustomInput({option}) {
    return(
        <span className="input">
            {option}
        </span>
    )
}